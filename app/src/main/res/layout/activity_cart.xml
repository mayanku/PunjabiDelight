<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="#CACDDF"
    android:layout_height="match_parent"
    tools:context=".CartActivity">

    <include
        android:id="@+id/toolbar2"
        layout="@layout/toolbar1"/>



    <TextView
        android:id="@+id/msg1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/colorPrimaryDark"
        android:layout_marginStart="10dp"
        android:layout_marginEnd="10dp"
        android:textAlignment="center"
        android:layout_centerVertical="true"
        android:visibility="gone"
        android:textSize="20dp"
        android:text="Congratulations, Your Final Order has been Placed Successfully. Soon You will receive your order at your doorstep "
        />
    <androidx.recyclerview.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/cart_list"
        android:layout_below="@+id/toolbar2"
        android:layout_above="@+id/next_process_btn">

    </androidx.recyclerview.widget.RecyclerView>

    <Button
        android:id="@+id/next_process_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="#5A6FB3"
        android:text="CHECKOUT"
        android:textColor="@android:color/white"
        android:textSize="20dp" />

</RelativeLayout>